/**
 * @deprecated Removed in the Planning workflow refactor — 0 importers.
 * Replaced by `product-plan.tsx` (ProductPlan) and `dealer-summary-view.tsx`
 * (DealerSummaryView), which render live from the shared PlanEditProvider.
 * Kept only because the sandbox blocked file deletion; safe to `git rm`.
 */
export {};
