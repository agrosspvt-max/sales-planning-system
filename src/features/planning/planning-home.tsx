/**
 * @deprecated Removed in the Planning workflow refactor — 0 importers.
 * Replaced by `planning-modules.tsx` (PlanningModules) rendered by the Create Plan and
 * View Plans workspaces. `/planning` now redirects to `/planning/create`.
 * Kept only because the sandbox blocked file deletion; safe to `git rm`.
 */
export {};
