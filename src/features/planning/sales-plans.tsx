/**
 * @deprecated Removed in the Planning workflow refactor — 0 importers.
 * Replaced by `sales-planning.tsx` (SalesPlanning with mode="view"), which renders the
 * approved-only list at /planning/sales/plans and shares the Create/View toggle.
 * Kept only because the sandbox blocked file deletion; safe to `git rm`.
 */
export {};
