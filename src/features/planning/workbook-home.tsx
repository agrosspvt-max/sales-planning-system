/**
 * @deprecated Removed in the Planning workflow refactor — 0 importers.
 * The standalone "pick a plan, view workbook" screen is superseded by the in-plan
 * Product Plan + Dealer Summary tabs. `/planning/sales/workbook` now redirects.
 * Kept only because the sandbox blocked file deletion; safe to `git rm`.
 */
export {};
