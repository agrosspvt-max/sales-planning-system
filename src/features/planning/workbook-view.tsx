/**
 * @deprecated Removed in the Planning workflow refactor — 0 importers (its only caller,
 * workbook-home.tsx, is also deprecated). Superseded by the in-plan Product Plan +
 * Dealer Summary tabs. Kept only because the sandbox blocked file deletion; safe to `git rm`.
 */
export {};
